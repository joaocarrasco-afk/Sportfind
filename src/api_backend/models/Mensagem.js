const {db, auth, realdb} = require('../factory/config');
const {addDoc, doc, getDoc, collection, query, getDocs, where, updateDoc} = require('firebase/firestore');
const {ref, set, onValue, push} = require('firebase/database');


class Mensagem{




}

module.exports = new Mensagem();